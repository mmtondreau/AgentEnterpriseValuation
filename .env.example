# Google API Key for Gemini models
GOOGLE_API_KEY=your_gemini_api_key_here

# EODHD API Key for financial data
EODHD_API_KEY=your_eodhd_api_key_here

# PostgreSQL Connection (when running via docker-compose)
DATABASE_URL=postgresql://postgres@localhost:5432/agent_state
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agent_state
DB_USER=postgres
# No password needed (trust authentication)

# MCP Server Configuration
MCP_HOST=0.0.0.0
MCP_PORT=8000
LOG_LEVEL=INFO
